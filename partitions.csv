# Name,   Type, SubType, Offset,   Size,     Flags
# Offsets are explicit so nvs, phy_init and factory keep the addresses the stock
# single-app table gave them: settings and the BLE bond database survive flashing
# this table. `anim` occupies the previously unallocated tail (docs/ble-protocol.md S3.1).
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  1M,
anim,     data, nvs,     0x110000, 0x10000,
